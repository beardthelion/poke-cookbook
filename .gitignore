# Local-only project notes (contains personal info / strategy — never publish)
project-context.md

# Local-only Claude Code working instructions (kept out of the public repo)
CLAUDE.md

# Claude Code session + local settings
.remember/
.claude/settings.local.json

# Local-only planning / design scratch (not part of the published project)
docs/superpowers/

# Cloudflare Wrangler local state (created by `wrangler pages dev`)
.wrangler/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
